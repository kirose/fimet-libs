package com.fimet.core.stress;

import java.io.File;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

import com.fimet.core.IMessengerManager;
import com.fimet.core.Manager;
import com.fimet.core.iso8583.parser.Message;
import com.fimet.core.net.IAdaptedSocket;
import com.fimet.core.net.IMessenger;
import com.fimet.core.net.IMessengerListener;
import com.fimet.core.net.ISocket;

public class TestBuilder {
	private Stress stress;
	public TestBuilder() {
		stress = new Stress();
	}
	public TestBuilder add(File stressFile, IAdaptedSocket socket) {
		injectors.add(new StressInjector(stressFile, socket));
		return this;
	}
	public void execute() {
		connector = new Connector(stress);
		connector.start();
	}
	public void _execute() {
		injector.startInjector();
	}
}
